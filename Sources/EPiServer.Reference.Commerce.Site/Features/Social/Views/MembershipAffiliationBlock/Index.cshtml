@using EPiServer.Web.Mvc.Html

@model EPiServer.Reference.Commerce.Site.Features.Social.Models.Groups.MembershipAffiliationBlockViewModel
<div class="border">
    @if (Model.ShowHeading)
    {
        <h2 @Html.EditAttributes(x => x.Heading)>@Model.Heading</h2>
        <hr />
    }

    @foreach (var message in Model.Messages)
    {
        var messageStyle = message.ResolveStyle(message.Type);
        <div style="color:@messageStyle">@message.Body</div>
    }

    @if (Model.Groups != null && Model.Groups.Any())
    {
        <div class="row">
            <div class="span6">
                <div>
                    <table class="table table-striped">

                        @foreach (var group in Model.Groups)
                        {
                            <tr>
                                <td>
                                    <a href=@HttpUtility.UrlDecode(group.PageLink)>
                                        <strong>@group.Name</strong>
                                    </a> 
                                </td>
                            </tr>
                            <tr>
                                <td>@group.Description</td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    }
</div>
